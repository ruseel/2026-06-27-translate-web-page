# Korean is the fixed target language

Translate WebPage will treat Korean as the fixed target language rather than a configurable first language. This keeps the reader experience, prompt design, data expectations, and translation commons focused instead of generalizing the system before the Korean article-reading loop is strong.
